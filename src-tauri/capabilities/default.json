{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacités VisioNode : core + deep-link + SQL locale + updater + dialog + process.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "sql:default",
    "updater:default",
    "dialog:default",
    "process:default",
    "process:allow-restart"
  ]
}
