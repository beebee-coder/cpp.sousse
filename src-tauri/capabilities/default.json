{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capacités VisioNode : core + deep-link + SQL locale embarquée.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "sql:default"
  ]
}
