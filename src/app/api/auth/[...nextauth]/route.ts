
/**
 * @fileOverview Désactivé. L'authentification est gérée par /api/auth/signin.
 * Ce fichier est conservé vide pour éviter les erreurs de dossier Next.js.
 */
import { NextResponse } from 'next/server';

export async function GET() { return new Response(null, { status: 404 }); }
export async function POST() { return new Response(null, { status: 404 }); }
